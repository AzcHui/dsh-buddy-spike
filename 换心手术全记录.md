# dsh 换心手术全记录

> **项目**：让 DeepSeek Harness（dsh）的 Web UI 使用腾讯 CodeBuddy 作为唯一大脑
> **结果**：✅ 全通（2026-09-23 09:36 实测）——dsh 的 UI/会话/插件生态 + CodeBuddy 的大脑，燃料纯腾讯积分，**DeepSeek 零消耗**
> **周期**：2026-09-22 ~ 2026-09-23（两个工作日，含全部排障）
> **代码位置**：`dsh-buddy-spike/`（本文档同目录）

---

## 一、一页速览

| 项 | 内容 |
|---|---|
| 目标 | dsh Web UI 对话由 CodeBuddy 回答，不再购买 DeepSeek token |
| 路线 | "单脑换心"：替换 dsh 默认 agent-loop 插件为自研 dsh-buddy-loop（内驱 `@tencent-ai/agent-sdk`） |
| 为什么可行 | ① dsh 官方明言"驱动器保持可替换"；② agent-sdk 自动复用 codebuddy CLI 登录态（积分走订阅） |
| 核心交付 | `buddy-loop/lib/`（插件）+ `buddy.patch.yml`（换心装配）+ junction 依赖链 |
| 最大坑 | dsh patch 语义：**patch 行不许换名**，name mismatch 整行静默跳过 |
| 验收证据 | Web UI 会话内 "我是 CodeBuddy Code…"，已思考+46K tok+8s，source.provider=codebuddy |

---

## 二、背景与动机

1. 用户买了腾讯 CodeBuddy/WorkBuddy 套餐，积分充足；dsh 默认走 DeepSeek API，需另购 token。
2. 调研结论（09-22）：
   - WorkBuddy/CodeBuddy **客户端**积分是闭环计费，无对外 API；
   - 腾讯云 Coding Plan（`api.lkeap.cloud.tencent.com/coding/v3`）是唯一官方外部调用方案，但要单独发 Key——不算"用上套餐积分"；
   - CodeBuddy 全家桶（IDE/插件/CLI）**均不开源**；
   - **决定性发现**：官方 `@tencent-ai/agent-sdk`（npm）存在，`query()` 返回流式消息，**认证自动复用 codebuddy CLI 登录态**——积分直扣订阅。
3. 用户定调"完全抛弃大脑1，直连一个大脑"：不做中间层翻译、不做双脑混合，**换心**。

### 为什么 dsh 允许这么干

dsh（DeepSeek Harness，MIT，cordis 插件内核）的设计哲学是"一切皆插件"：

- `dsh-agent-loop` 只是 Agent 约定的**默认出厂驱动器**，不是地基；
- `dsh-agent` 契约明示"驱动器保持可替换"；
- `ctx.agents.setFactory()` / `ctx.agents.create()/resume()` 全部委托给已注册工厂；
- 装配通过 `cordis.patch.yml` 分层覆盖（bundle 层 → profile 层 → `--patch` 覆盖层，后者胜出）。

---

## 三、最终架构

```
浏览器 ──HTTP──> dsh web (port 3210)
                   │
                   ├─ dsh 全家桶插件：会话/UI/系统提示词/skill-catalog/上下文注入…
                   │
                   └─ dsh-buddy-loop（我们写的，接管 agentLoop 服务）
                        │  每 turn = 一次 SDK query()（流式）
                        ▼
               @tencent-ai/agent-sdk
                        │  自动复用 CLI 登录态
                        ▼
               codebuddy CLI（已登录）──> 腾讯套餐积分
                                        （DeepSeek 零消耗）
```

**事件流映射**（CodeBuddy SDK → dsh session）：

| SDK 消息 | dsh 事件/处理 |
|---|---|
| `stream_event` + `text_delta` | `text-delta` 流帧 → `agent/assistant-stream` |
| `stream_event` + `thinking_delta` | `reasoning-delta` 流帧（UI 显示"已思考"） |
| `assistant`（完整消息） | 兜底文本（无部分流时一次性提交） |
| `result` | durable `assistant/message`（source.provider=codebuddy）+ usage 映射 |

---

## 四、里程碑时间线

| 阶段 | 时间 | 内容 | 结果 |
|---|---|---|---|
| 调研 | 09-22 上午 | 腾讯生态积分调用方式全扫 | 确定官方 SDK 路线 |
| Spike 1-3 | 09-22 | CLI 登录态/SDK 单发/dsh 插件树侦察 | 全过（Spike-2 沙箱被 auth 墙挡，移师用户终端） |
| Stage 1 | 09-22 17:31 | `dsh-buddy-loop` 插件 + `run-buddy.mjs` 最小回路 | ✅ PASS：完整事件流，CodeBuddy 在 dsh 会话内作答 |
| Stage 3 | 09-22 17:57 | patch 装配 + Web UI 接入（跳过 Stage 2，用户要求先见成果） | 沙箱推进到 entry 108+ 卡在删除守卫，交用户终端 |
| 排障 | 09-23 09:03-09:27 | 四连障（详见第六章） | 全清 |
| **全通** | **09-23 09:36** | **Web UI 新会话 → CodeBuddy 流式回答** | ✅ 46K tok / 8s / DeepSeek 零消耗 |
| 版本调研 | 09-23 10:30 | npm 版本线 + GitHub release 全扫 | 选定 0.1.5-rc.3（next），69 依赖精确锁 |
| 源码迁移 | 09-23 11:10-12:30 | checkout rc.3 → pnpm install/build（四连坑，详见第九章） | ✅ 构建成功 |
| 换心重挂 | 09-23 13:30-13:45 | buddy-loop 逐包 junction 重指源码树（cordis 单例保障） | ✅ 7/7 解析通过 |
| **源码版全通** | **09-23 14:05** | **rc.3 Web UI → CodeBuddy 回答** | ✅ 53.7K tok / 11s |

---

## 五、交付物清单

```
dsh-buddy-spike/
├── buddy.patch.yml              # 换心装配 patch（核心三行 + 注释详尽）
├── buddy-loop/                  # dsh-buddy-loop 插件本体
│   ├── package.json             # type: module, main: lib/index.js
│   └── lib/
│       ├── index.js             # BuddyLoop 工厂服务（createAgent/resume/持久化）
│       └── agent.js             # BuddyAgent 驱动器（SDK query 流→dsh 事件 + QueueInbox）
├── dsh-recon/                   # dsh 全量依赖树工作区（290+ 插件包）
│   ├── node_modules/dsh-buddy-loop  → junction → ../buddy-loop
│   └── run-buddy.mjs            # Stage 1 最小回路验证脚本
└── dsh-home/                    # 圈定的 DSH_HOME（profiles/web + sessions + storages）
```

**启动命令**（用户 PowerShell）：

> 2026-09-23 下午起**主用源码构建版 rc.3**（见第九章）；npm 包版 0.1.5-alpha.2 保留作回滚基线。

源码版（当前主用）：

```powershell
cd <源码仓库目录>        # 源码仓库，HEAD 在 dsh-v0.1.5-rc.3
$env:DSH_HOME = "<本包目录>\dsh-home"
node apps\cli\lib\bin.js web --patch <本包目录>\buddy.patch.yml --port 3210
# 浏览器打开 http://127.0.0.1:3210
```

npm 包版（回滚基线）：

```powershell
cd <本包目录>\dsh-recon
$env:DSH_HOME = "<本包目录>\dsh-home"
node node_modules\@deepseek-ai\dsh\lib\bin.js web --patch ..\buddy.patch.yml --port 3210
```

**buddy.patch.yml 核心逻辑**（最终正确形态）：

```yaml
# 1. 禁用原生 DeepSeek 驱动器（不带 name，干净生效）
- id: agent-loop
  disabled: true

# 2. insert 新行挂 CodeBuddy 驱动器（服务名同为 agentLoop，消费者无感）
- insert:
    - id: buddy-loop
      name: ./buddy-loop/lib/index.js   # 相对 patch 文件定位（关键！）
      config:
        buddyMaxTurns: 30
        agents: []

# 3/4. 禁用 llm-pi-ai（依赖闭包巨大且休眠）与 session-title-llm（要 DeepSeek key）
```

---

## 六、踩坑全记录（核心章节）

> 按类别分组。每个坑：现象 → 根因 → 解决 → 教训。

### A. 协议/装配类（最有价值，官方文档没写的）

#### 坑 1：patch 换名静默失效（Stage 3 第一障）

- **现象**：Web UI 正常启动，但对话报 `llm-deepseek: no API key for provider route "deepseek-official"`——换心没生效，原生驱动器还在跑。
- **根因**：`cordis-plugin-include` 的 `applyEntryPatches` 有这样一段——
  ```js
  if (name && name !== target.name) {
      warn("patch: name mismatch for %C (expected %C, got %C), skipping", ...);
  }
  ```
  patch 行如果指定了与目标行不同的 `name`，**整行被跳过**，只在 boot 时留一条 loader warning。我们最初写的 `id: agent-loop` + `name: dsh-buddy-loop` 就这么被静默扔掉了。
- **解决**：patch 不能改名。正确姿势 = 禁用旧行（`disabled: true`，不带 name）+ `insert` 新行挂新插件（服务名保持 `agentLoop`，消费方 dsh-agent 无感）。
- **教训**：**patch 的语义是"改字段"或"插行"，不是"换件"**。静默 skip + 仅 warning 的失败模式，必须盯 boot 输出的 warning 行。

#### 坑 2：loader 裸包名解析锚点在 profile 目录

- **现象**：修完坑 1 后 boot 直接炸：`Cannot find package 'dsh-buddy-loop' imported from ...\dsh-home\profiles\web\`。
- **根因**：loader 按包名 import 时，Node 的 node_modules 上溯从 **profile 目录**（`$DSH_HOME/profiles/web`）开始，永远走不到 `dsh-recon/node_modules`——我们的插件装在另一棵树里。
- **解决**：dsh patch 装载器有 `anchorInsertedPluginNames`：`name` 写 `./` 相对路径会被换算成**以 patch 文件所在目录为基准**的绝对 file URL。patch 和 buddy-loop 同在 `dsh-buddy-spike/`，写 `name: ./buddy-loop/lib/index.js` 正好指到位；插件内部依赖再经自己的 `node_modules` junction 落回 `dsh-recon/node_modules` 解析。
- **教训**：**bare specifier 的解析基准是"谁的 runtime 谁做主"**，跨树引用要么进对方的 node_modules，要么用会被锚定的相对路径。

#### 坑 3：cordis fiber 生命周期（Stage 1）

- **现象**：插件挂上后 service 查找失败/时序错乱。
- **根因**：cordis fork 是 fiber 生命周期，**root await 不等子 fiber**；插件必须逐个 `await fiber.await()` 到 state 2 再挂下一个。
- **解决**：`run-buddy.mjs` 里顺序 await 各插件 fiber 后再挂 BuddyLoop。
- **教训**：cordis 的"注册即启动"容易让人以为是同步装配，实际是状态机。

#### 坑 4：registry 调用方重追踪（`this.ctx` 被偷换）

- **现象**：`createAgent` 内部 `this.ctx` 缺注入（sessions 等）。
- **根因**：registry.create 用**调用方的 ctx** 重追踪工厂方法（getTraceable receiver），`this.ctx` 变成了调用方的上下文。
- **解决**：构造时把真实插件 ctx 存进不可代理实例字段：
  ```js
  Object.defineProperty(this, '_loopCtx', { value: ctx, enumerable: false, writable: false });
  ```
- **教训**：代理/追踪框架下，关键上下文要主动"逃生舱"固化。

### B. Web 层合同面类（隐含接口，报错才知道）

#### 坑 5：resume 未实现 → 旧会话打不开

- **现象**：`resume failed for session "session-ea68…": dsh-buddy-loop: resume is not supported yet (stage 1)`。
- **根因**：上次原生大脑创建的会话，Web UI 打开走 `factory.resume()` 通道，Stage 1 的我们直接 throw。
- **解决**：实现完整 resume：`persistence.open(id,'write')` → `handle.read(0)` 冷读 → `interruptedTurnClosers()` 补闭合中断的 turn → 历史作 seed 建 session → 发布驱动器。
- **教训**：替换框架默认件时，**工厂接口的每个方法都是合同**，先枚举再实现。

#### 坑 6：`agent.inbox` 缺失 → 新会话发消息炸

- **现象**：新会话发消息报 `Cannot read properties of undefined (reading 'nextTurn') (gateway/internal)`。
- **根因**：web 网关（api-session-controller）读写 `agent.inbox.nextTurn/nextStep/replace/remove`——官方 `ReactLoopInbox` 在 dsh-agent-loop 内部**不导出**。
- **解决**：自实现 `QueueInbox` 内存门面，覆盖消费面：`nextTurn`/`nextStep`/`hasPending`/`replace`/`remove`/`prepend`/`append`/`clear`；队列改为 `{ target, message }` 条目结构。
- **教训**：到这一步 Agent 隐含合同已经倒逼出三块：基础面（send/followup/steer/inject/cancel/whenIdle/status）→ resume → inbox。**Web 层深度耦合的面，报一个补一个，补前先全量 grep 消费方**。

### C. 持久化语义类（Stage 2 提前完成时踩的）

#### 坑 7：write handle 兼任活事件路由器

- **发现**：jsonl 后端全局挂 `ctx.on("session/event")`，但路由是 `writers.get(session.id)?.enqueueLive(...)`——**没有打开的 write handle，事件直接不落盘**（静默丢弃）。
- **推论**：① create 必须先 `persistence.create(session.header)` 拿所有权再发布；② resume 打开的 handle 必须持有到 agent dispose；③ **发布前的 appends 不走全局事件路由**，要手动 `appendUnstoredSuffix`（`session.snapshotEvents(storedCount)`）flush 进 handle。
- **教训**：框架的"自动持久化"其实有一条所有权链，替换默认件时所有权要自己接。

#### 坑 8：沙箱陈旧锁文件

- **现象**：用户终端 boot 报 `atomic-write: timed out waiting for the writer lock at ...\dsh-home\.credentials.yaml.lock`。
- **根因**：前一天 sandbox 里被删除守卫打断的 boot 留下的哨兵锁，进程已死锁未释。
- **解决**：删锁文件即愈。顺带发现 sandbox 的 safe-delete 拦工作区删除，需 PowerShell `Remove-Item` 直删。
- **教训**：**看到 "timed out waiting for ... lock" 先查锁文件时间戳**，九成是陈锁不是竞争。

### D. 环境/工具链类（一次性，但耗时最多）

| 坑 | 现象 | 解决 |
|---|---|---|
| dsh rc 版发布损坏 | 最新 rc.2/0.1.7-alpha.1 的传递依赖 `dsh-client-ui-sidebar-documentpreview@^0.1.5-rc.3` 在 npm 上不存在 | package.json `overrides` 钉死 0.1.5-alpha.2 / rc.2 |
| npm arborist 崩溃 | overrides + junction 组合下 `Invalid Version` | 弃用 npm，手动 tarball 安装 |
| zod 装坏 | 手动解压后不完整 | 重新下载 zod-4.6.5.tgz 替换 |
| 70 个残缺包 | npm 中断安装留下无 package.json 的残骸（3 个嵌套 yaml 挡住父级真包、ajv/hono 断裂、64 个跨平台可选二进制残骸） | 手写 Node 脚本全树扫描清理 + tarball 补 yaml@2.9.1、ajv@8.20.0 |
| pi-ai 依赖闭包巨大 | `dsh-llm-pi-ai` 拖 openai/anthropic/aws 全家桶 | patch 里 `disabled: true`（本就休眠挂载，零 routes） |
| 沙箱 auth 墙 | sandbox 内 codebuddy CLI 读不到登录态（initialize 超时） | **涉及凭据的验证一律用户终端执行** |
| 沙箱 coreutils 残缺 | bash 缺 dirname/head/sed 等 | Windows 下统一 PowerShell + 输出落盘 |
| junction 链接 | 插件跨树依赖解析 | `dsh-recon/node_modules/dsh-buddy-loop` → `buddy-loop`；`buddy-loop/node_modules` → `dsh-recon/node_modules`（双层互指） |

---

## 七、关键技术发现浓缩（可复用）

### dsh patch 语义速查（`applyEntryPatches`）

| 规则 | 说明 |
|---|---|
| 层序 | bundle 层 → profile `cordis.patch.yml` → `--patch` 覆盖层，**后者按 id 胜出** |
| 非 insert patch | 按 `id` 寻址，把其余字段**赋值覆盖**到目标行（config 整体替换、disabled 置位） |
| **禁止换名** | patch 带 `name` 且 ≠ 目标行 name → 整行 skip，仅 loader warning |
| `insert` | 无 id 追加到根列表；有 id 插入 group；插入行被索引，后续 patch 可寻址 |
| `!!js` 表达式 | 支持 `process.env`、`dshHomePath()` 等 |
| config dump | `dsh --dump-config` 可离线预览最终装配结果（与 boot 同一算法） |

### 持久化所有权链

```
persistence.create(header) / open(id,'write')   # 单写者原子 claim
    → handle 兼任活事件路由器（不开 = 不落盘）
    → 发布前 appends 不走路由，须手动 snapshotEvents + handle.append
    → dispose 时 handle.close()（drain 缓冲 + 释放 claim）
resume: open → read(0) → interruptedTurnClosers 补闭合 → 历史作 seed
```

### Agent 契约面（被 web 层倒逼出的完整清单）

| 层 | 必需面 |
|---|---|
| 基础驱动 | `send/followup/steer/inject/cancel/whenIdle/runMaintenance` + `status` + 事件（agent/status、agent/error、agent/assistant-stream） |
| Web 会话恢复 | `factory.resume(ownerCtx, { resumeSessionId, agentOptions, setup, signal, parentAgent })` |
| Web 收件箱 | `agent.inbox.{nextTurn, nextStep, hasPending, replace, remove, prepend}` |
| 工厂 | `createAgent(ownerCtx, { sessionId, seed, meta, inheritedEventCount, agentOptions, setup, signal, parentAgent })` + 返回 `{ agent, dispose }` |

---

## 八、已知限制与路线图

**当前限制**（都不影响日常对话）：

1. `steer` 只是排队成下一轮，没有真正的中途打断（Stage-1 简化）
2. CodeBuddy 的工具审批走它自己的审批流，未映射到 dsh 的 approval 弹窗
3. resume 后旧历史不喂给 CodeBuddy 上下文（每轮独立 query；UI 历史完整，模型不记得）
4. 会话标题生成已禁用（原本要 DeepSeek key）
5. dsh 工具未通过 MCP 桥喂给 CodeBuddy（各跑各的工具）

**后续迭代方向**：

- [ ] PermissionRequest Hook → dsh 审批 UI 映射
- [ ] 全量历史回放（session 事件 → SDK 多轮上下文）
- [ ] session-title buddy 化（用积分生成标题）
- [ ] MCP 桥：dsh 插件工具注册进 codebuddy
- [ ] 正式收敛为可安装的独立插件包（脱离 spike 目录）

---

## 九、二次远征：迁移到源码版 0.1.5-rc.3（09-23 下午）

> 上午全通后，决定把宿主从 npm 包 0.1.5-alpha.2 升级到源码构建的 rc.3（防依赖漂移 + 拿 Web 插件面板 API 等新地基建做配置可视化）。

### 9.1 版本决策记录

| 候选 | 状态 | 结论 |
|---|---|---|
| 0.1.5-rc.2（latest） | 09-10 发布，无 changelog 增量 | 稳但旧 |
| **0.1.5-rc.3（next）** | 09-22 发布；**69 个依赖全部去 `^` 锁精确版本**——正面封死我们上周踩的"依赖漂移断树"坑 | **选定** |
| 0.1.7-alpha.2 | 日更节奏；两处 breaking 打在 buddy-loop（`agent/session-start`→异步 `agent/created`、弃用同步 `snapshotEvents`） | 等 0.1.6/0.1.7 进 rc 再一步到位 |

源码仓库 `<源码仓库目录>`（pnpm monorepo，290 workspace 项目，`apps/cli` = `@deepseek-ai/dsh`）。**切版本 = `git checkout dsh-v0.1.5-rc.3`，不需要删仓库重下**。

### 9.2 源码构建四连坑

#### 坑 9：git checkout 超时被杀 → index.lock 残留

- **现象**：前台 `git checkout`（alpha.2 → rc.3 是几万文件回退）两次被 120s 超时 SIGTERM，第二次留下 `.git/index.lock` 卡死后续 git 操作。
- **解决**：清锁后改后台执行，实测 **16 分 41 秒**（Windows 文件系统 + TortoiseGit TGitCache 实时扫描拖累）。
- **教训**：monorepo 大跨度切 tag 前先暂停 TGitCache；长文件操作一律后台跑。

#### 坑 10：pnpm 自管理链接丢失 → `MODULE_NOT_FOUND node_modules/pnpm/bin/pnpm.mjs`

- **现象**：`pnpm run build` 报错——build 脚本内部再调 pnpm 时，PATH 里的 `node_modules/.bin/pnpm.CMD` shim 加载不到实体。
- **根因**：仓库 `packageManager: pnpm@11.7.0` 触发 pnpm 自管理（把自己装进 `node_modules/pnpm`）；沙箱内那次 install 被删除守卫打断，`.pnpm/pnpm@11.7.0` 实体完好但顶层链接没建。
- **解决**：`New-Item -ItemType Junction` 重建 `node_modules/pnpm → .pnpm/pnpm@11.7.0/node_modules/pnpm`。
- **教训**：**pnpm run 内部再调 pnpm 走的是项目本地副本**；报错 requireStack 为空 + 路径在 node_modules 里，先查链接完整性。

#### 坑 11：60 个"幽灵目录" → tsdown `Cannot find entry [@dsh-root]`

- **现象**：build 在 tsdown 配置解析阶段炸，报 `[dsh-root] Cannot find entry lib/types/{index,invariant,startup}.js`，且失败序号每次不同（39/43）。
- **根因**：切 tag 时 git 只删**被跟踪文件**——0.1.6/0.1.7 存在而 rc.3 已移除的包（`packages/ssh/*`、`experimental/*` 等）留下只含 `lib/` + 未跟踪 `node_modules/` 的空壳目录 ×60。tsdown 按目录模式扫描把空壳当包 → 无 package.json → 继承根 entry → 炸。`[dsh-root]` 前缀只是项目名缺失时的 fallback；失败序号漂移是因为 `Promise.all` 并行解析。
- **解决**：批量删除"glob 匹配目录中无 package.json 者"，清零后构建通过。
- **教训**：**monorepo 降级/切 tag 后必须清未跟踪构建残渣**；官方 CI `clean: true` 全新 checkout 永远踩不到这类坑。

#### 坑 12：陈旧 lib 产物 → `volatile is not a function`

- **现象**：构建成功但 boot 炸在宿主自家插件 `ui-settings`：`DeveloperToolsSettingsFields.enabled.volatile is not a function`。
- **根因**：`lib/` 同样 gitignore——切 tag 后残留 0.1.7 时代产物；tsc 增量构建凭 tsbuildinfo 判定"免重编"，tsdown 把**旧 tsc 产物**打进了新包（旧源码用 `.volatile()`，rc.3 已移除）。铁证：今天刚构建的 `lib/index.js` 里有 `volatile`，当前 `src/` 里 grep 不到。与坑 11 同属"脏 checkout"病。
- **解决**：`pnpm clean`（官方 `tsx scripts/clean.ts`，按 TS 工程图清 lib/.typecheck/孤儿 node_modules）→ `pnpm run build` 全量重编。
- **教训**：**切版本后的第一次构建前先 clean**；验证产物是否陈旧的方法——grep 产物与 src 对质。

### 9.3 buddy-loop 重挂：从"单 junction"到"逐包 junction"

宿主换树后，buddy-loop 原来那条 `node_modules → dsh-recon/node_modules`（alpha.2 树）会引入 **cordis 双实例**（宿主用 vendor 源码版、插件用 npm 4.0.2），必炸。重挂方案：

```
buddy-loop/node_modules/            # 从单 junction 改为真实目录
└── @deepseek-ai/
    ├── cordis       → apps/cli/node_modules/@deepseek-ai/cordis（→ vendor/cordis）
    ├── dsh-agent    → …/dsh-agent（→ packages/core/agent）
    ├── dsh-llm      → …/dsh-llm（→ packages/llm/llm）
    ├── dsh-session  → …/dsh-session（→ packages/core/session）
    ├── dsh-scope    → packages/core/scope（apps/cli 顶层没有，直指 workspace 包）
    └── schemastery  → …/schemastery（→ vendor/schemastery）
```

**三个关键事实**：

1. **cordis 单例验证**：宿主 bin.js 与 buddy-loop 解析到同一个 `vendor/cordis/lib/index.js` ✓（require.resolve 双向对比）
2. **schemastery 是 `Symbol.for("schemastery")` 全局单例，先加载者称王**——插件与宿主必须同源，否则全局 Schema 被旧版本污染
3. **`@tencent-ai/agent-sdk` 不在任何依赖树里**，位于 spike 根 node_modules，靠 Node 上溯解析（解析链 7/7 验证通过）

另：`--preserve-symlinks` **不要用**——会破坏 pnpm symlink 解析语义，宿主自己都起不来；patch 入口是绝对路径直指，junction 由文件系统层透明解析。

### 9.4 rc.3 对换心插件的兼容性核验（全绿）

| 核验项 | 结果 |
|---|---|
| patch 目标行（`agent-loop`/`session-title-llm`/`dsh-llm-pi-ai`） | ✅ 全在 base patch 原位 |
| `SessionPreparation`/`interruptedTurnClosers`/`SessionLogOffset` | ✅ dsh-session 导出健在 |
| `emitAgentEvent`/`agentEvents` | ✅ dsh-agent 健在 |
| `BlockAssembler`/`AssistantStreamAccumulator`/`StreamChunk` | ✅ dsh-llm 健在 |
| 实测 | ✅ 09-23 14:05 Web UI 对话，53.7K tok 走积分 |

**附带收益**：rc.1 起会话格式 V3 自动迁移（保留原文件）、Web 插件面板 API（方案一"配置可视化页"的官方路）、Windows 子进程不弹窗、断线自动恢复。

---

## 十、经验总结（元方法论）

1. **先侦察后动手**：Spike 三连（登录态/单发/插件树）花掉半天，换来后面每一步都有据可依。
2. **报错即路标**：今天的四连障，每一个报错都精确指向下一层合同——`no API key`（patch 没生效）→ `ERR_MODULE_NOT_FOUND`（解析锚点）→ `resume is not supported`（我们自己的 throw，证明挂载成功）→ `nextTurn undefined`（inbox 合同）。
3. **框架源码是唯一真相**：patch 语义、持久化所有权、inbox 消费面，全是 grep 进 node_modules 里读出来的，官方文档一个字没写。
4. **静默失败最危险**：patch name mismatch 只发 warning 不报错——**boot 输出里的 warning 行和 error 行同等重要**。
5. **沙箱与真实环境的边界要清醒**：涉凭据、涉删除守卫的验证，尽早移交用户终端，不在 sandbox 里缠斗。

---

*文档生成：2026-09-23，基于 `.workbuddy/memory/2026-09-22.md` 与 `2026-09-23.md` 工作日志整理；同日晚间补充第九章（源码版 rc.3 迁移）。*

---

## 十一、运行期事故全记录（09-24，验收后浮现的三个 bug）

> 手术验收通过 ≠ 万事大吉。本章记录换心后真实使用中暴露的问题——每个都按
> 「现象 → 取证 → 根因 → 修复」四段式归档，排障时先来这里查历史。

### 事故 1：会话历史"一片空白"（turn 撞号）— commit 08cb3e9

- **现象**：刷新后聊天窗只剩"你好，你是谁"一轮，点「轨迹」再返回才出现部分记录
- **取证**：多帧 zstd 解码 session.v3.jsonl.zstd——4 轮对话全部完整落盘（数据层零丢失）；全部 `turn:1`
- **根因**：BuddyAgent 把起点硬编码 `turn:0`，收尾还丢弃编号；原生 agent-loop 是从 turnBoundary 投影恢复 lastTurn 跨重启单调递增的。聊天窗按 turn 号组装，N 个"turn 1"撞在一起被折叠
- **修复**：构造时扫会话日志恢复最大 turn 号（比投影更稳，投影缓存丢了也不怕）；wakeDriver 延续 `_lastTurn`；kick 收尾回写
- **教训**：移植原生逻辑时"内存态字段"都有对应的持久化恢复机制，漏一块就炸一块

### 事故 2：每轮"失忆开局" — commit 09f1927

- **现象**：用户感觉"每次发消息像新会话"；模型自述"这是对话的第一条消息"
- **取证**：解码 assistant 内部推理文本直接暴露 "This is the very first message"；dsh 侧日志却在正常积累——写入健康、上下文断裂
- **根因**：`query({ prompt })` 缺省只传当前消息，历史从没喂回 CLI；SDK 的 `sessionId`/`resume` 能力当时没接
- **修复**：dsh 会话 id（UUID 过 SDK 校验）作 CodeBuddy sessionId，1:1 映射：首轮 create、之后 resume（init 消息回传的 CLI id 优先）；resume 失败且零流式输出→降级新建重试
- **教训**：SDK 的默认行为 ≠ 合理行为；接入任何 SDK 先把"会话/状态生命周期"的选项面过一遍

### 事故 3：resume 挂死（"深度求索中..."永转圈）— commit f716f7d

- **现象**：发消息后永久 spinner；日志铁证 turn 4 落盘 turn/start + user/message + step/start 后零事件
- **根因**：CodeBuddy CLI **resume 不存在的转录时静默挂死、不报错**——事故 2 修复的重启首轮恰好踩中（`_lastTurn>0` 触发 resume，但历史轮次是修复前跑的，CLI 侧没有对应转录）
- **修复**：双层防御——①resume 前置校验 `~/.codebuddy/projects/*/<id>.jsonl` 存在（探测失败宁失忆不挂死）②60 秒启动看门狗（abortController 透传 SDK，收到首条消息解除），异常挂法降级为可见失败
- **教训**：**降级路径本身也要有超时**；"等错误来触发降级"在对方不报错时就是死锁

### 环境陷阱备忘（取证时反复踩）

| 陷阱 | 正确姿势 |
|---|---|
| `zstdDecompressSync` 只解第一帧 | 按魔数 `28 B5 2F FD` 切帧逐帧解 |
| 会话按 `$DSH_HOME` 隔离 | 排障第一步：`echo $env:DSH_HOME` |
| PowerShell `*>` 落盘是 UTF-16 | 先解码再 grep |
| zstd 魔数字节序 | 文件里是 `28 b5 2f fd` 正序 |

## 十二、运行日志（黑匣子）——排障第一步看这里

09-24 起 buddy-loop 全链路落结构化日志（JSON Lines），位置：

```
$DSH_HOME/logs/dsh-buddy.log        # 当前（超过 5MB 自动轮转为 .old）
```

| 事件 | 含义 | 排障看点 |
|---|---|---|
| `agent-created` | BuddyAgent 构建 | `recoveredLastTurn` 是否等于上轮编号 |
| `turn-start` / `turn-end` | 回合起止 | 有 start 没 end = 挂死或崩溃 |
| `query-start` | 发起 CLI 查询 | `mode`=create/resume、`resumeId`、`model` |
| `cli-init` | CLI 握手成功 | 距 query-start 的延迟（卡死的分水岭） |
| `first-chunk` | 首个流式块 | init 到首块的模型延迟 |
| `query-result` | CLI 结果 | `subtype`/`isError` |
| `query-failure` | 查询抛错 | `mode` + message |
| `query-fallback` | resume 降级新建 | `from`（哪个 id 的转录失效） |
| `model-selection` | UI 选模型生效 | provider/model |

**排障速查流程**：①先看本表定位最后一个出现的日志事件 ②对照会话日志（`~/.dsh/sessions/`，多帧 zstd）确认数据层 ③CLI 侧转录在 `~/.codebuddy/projects/<cwd-slug>/` ④还不明就里，F12 Network 看红色失败请求。

---

## 十三、换心 agent 工具清单风险盘点（09-24 下午）

换心后 agent 拿到的是 CodeBuddy CLI 的完整工具集（文件五件套 / Bash+PowerShell / 任务管理 / WebFetch+WebSearch / 计划模式 / 子代理 / ToolSearch 延迟工具等）。对照 `_handleToolPermission` 桥的实现逐一核对，结论：**"卡死"类风险已从架构上封死，剩余均为有声响的失败**。

### 13.1 三重兜底（为什么不会卡死）

| 机制 | 说明 |
|---|---|
| 失败关闭 | 桥内任何异常（服务缺失 / 字段缺失 / 审批面不可用）→ 统一 `deny + message`，模型收到拒绝原因自行改道，回合不中断 |
| 看门狗 | 只武装到 **CLI 首条消息**为止，收到 init（实测 2-3s）即解除；工具执行多久都不管，不会误杀长任务 |
| 沙箱拒绝 | 沙箱拦截命令（如 Add-Type）是报错回给模型，不是挂起 |

所有工具（含 ToolSearch/DeferExecuteTool）都经过 `_handleToolPermission`，无一漏网。

### 13.2 四个留意项（语义错位类，报错/弹窗而非挂死）

| 风险 | 触发条件 | 表现 | 处置 |
|---|---|---|---|
| ① ExitPlanMode 循环 | 用户对"计划批准"点拒绝 | 模型留在计划模式反复重提计划，来回弹审批 | 不想执行直接说"退出计划"，别点拒绝 |
| ② 子代理权限继承 | agent 用 `Agent` 派子代理 | 若子代理调用不过桥 → 重现 "No permission handler" 报错（报错不挂） | 待实测：派子代理干活后翻黑匣子查 `tool-permission` |
| ③ 多问题映射 | AskUserQuestion 一次 2+ 题 / "Other" 自由输入 | 答案按问题文本做键回填，多题未实战验证（单题已过） | 待实测 |
| ④ 后台任务交叉 | Cron / loop / Monitor / 后台 Bash 与聊天同时跑 | 转录和 turn 编号可能交错 | 尽量不在会话内开定时任务 |

### 13.3 能力差异（无害）

Artifact 预览、PushNotification 等需要宿主 UI 的工具在 dsh web 无对应面板，产出以文本呈现；WaitForMcpServers 无 MCP 时空转返回。

**总体判断**：挂死（静默无响应）这类最难排的问题已消灭；剩余风险全部**有声响**，黑匣子一翻便知。收尾实测建议：②③各一分钟，①④记住即可。

---

*第十一章、十二章：2026-09-24 补充，基于当日 `.workbuddy/memory/2026-09-24.md` 工作日志整理。第十三章：2026-09-24 下午工具清单风险盘点。*
